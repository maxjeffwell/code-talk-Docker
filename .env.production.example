# Docker Compose Production Environment Variables
# Copy this file to .env.production and update with your values

# Node Environment
NODE_ENV=production

# PostgreSQL Configuration
# IMPORTANT: Use strong passwords in production!
POSTGRES_DB=code_talk_production
POSTGRES_USER=code_talk_user
POSTGRES_PASSWORD=CHANGE_ME_strong_postgres_password_here

# Redis Configuration
# IMPORTANT: Use a strong password!
REDIS_PASSWORD=CHANGE_ME_strong_redis_password_here

# JWT Secret
# IMPORTANT: Must be 64+ characters, cryptographically random
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_generate_a_very_long_random_jwt_secret_minimum_64_characters

# CORS Configuration
# IMPORTANT: Update with your production domain(s)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Client GraphQL Endpoints
# IMPORTANT: Update with your production domain
REACT_APP_GRAPHQL_HTTP_URI=https://yourdomain.com/graphql
REACT_APP_GRAPHQL_WS_URI=wss://yourdomain.com/graphql

# Docker Image Version
# Use semantic versioning or git tags (e.g., v1.0.0, v1.2.3)
VERSION=latest

# Security Notes:
# 1. NEVER commit .env.production to version control
# 2. Use strong, unique passwords for all services
# 3. Rotate secrets regularly
# 4. Use different credentials for dev and production
# 5. Keep JWT_SECRET at least 64 characters
# 6. Configure SSL/TLS for production (see KUBERNETES.md or DOCKER.md)
